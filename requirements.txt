# Core runtime — everything needed to RUN the app (on the bundled SQLite databases)
# and to run the full Specmatic test suite. Verified on Python 3.11 through 3.14.
#
# The external database drivers (Postgres, MySQL, MSSQL, Oracle, Mongo, Cassandra,
# Redis) are OPTIONAL and live in requirements-optional.txt. They are all
# lazy-imported by their adapters, so the app and the entire test suite run without
# them — install requirements-optional.txt only if you want to connect those engines.
# (Pinned loosely so a newer Python, e.g. 3.14, always resolves a compatible wheel.)

# Web framework
Flask>=3.1,<4
flask-cors>=5,<7
python-dotenv>=1,<2

# AI / LLM
groq>=0.25,<1

# Utilities
requests>=2.31,<3
cryptography>=43
python-pptx>=1,<2
faker>=40
